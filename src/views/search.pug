extends base
include mixins/video

block content 
    div.search
        form(method="GET").search__form
            input(placeholder="검색", type="text", name="keyword")
            input(type="submit", value="🔍")
        div.video-grid
            each video in videos 
                +video(video)
            else
                span.empty__message No videos found